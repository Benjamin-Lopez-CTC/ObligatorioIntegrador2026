#!/usr/bin/env node
import{main}from"./run.js";process.on("SIGINT",()=>process.exit(130));try{process.exit(await main(process.argv.slice(2)))}catch(e){process.stderr.write(`${e instanceof Error?e.stack??e.message:String(e)}
`);process.exit(1)}